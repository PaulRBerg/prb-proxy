uninstallPlugin.t.sol
├── when the caller does not have a proxy
│  └── it should revert
└── when the caller has a proxy
   ├── when the plugin is unknown
   │  └── it should revert
   └── when the plugin is known
      ├── it should uninstall the plugin
      ├── it should remove the methods from the reverse mapping
      └── it should emit an {UninstallPlugin} event
